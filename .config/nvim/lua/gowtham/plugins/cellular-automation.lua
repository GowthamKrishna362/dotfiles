return {
  'eandrju/cellular-automaton.nvim',
  config = function()
    -- No specific configuration is needed for basic usage.
    -- Add any additional setup here if required in the future.
  end,
}
